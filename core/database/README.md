# 🗄️ Database Layer

This folder manages the storage and retrieval of application data.

### 📄 Files
- **`db_config.py`**: Configuration for SQLite connections and table initialization.

### ⚙️ How it works
It manages the `users.db` file located in the `data/` directory. It provides helper functions to initialize the database and create a secure connection for the authentication routes.
